# Load required packages
# install.packages("tidyverse")
# library(tidyverse)

print("Hello world")

area_hectares <- 2.5                # land area in hectares
area_acres <- 2.47 * area_hectares  #convert to acres
area_acres                          # print

# Excercise - changes not made until you run the full script
length <- 5
width <- 50
area <- length * width
area

b <- sqrt(a)

no_membrs <- c(3,7,10,6) # "c" is to combine into a vector or concatenate
length(no_membrs)
class(no_membrs)
str(no_membrs) # str = structure

respondent_wall_type <- c("mud", "brick", "wood")
length(respondent_wall_type)
class(respondent_wall_type)
str(respondent_wall_type)

possessions <- c("bike", "radio", "TV")
possessions <- c(possessions, "phone") # add another element to vector
possessions <- c("car", possessions) 




